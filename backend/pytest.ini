[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short --strict-markers
markers =
    auth: Authentication tests
    incidents: Incident CRUD tests
    notifications: Notification tests
    analytics: Analytics tests
